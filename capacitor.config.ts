import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.starter',
  appName: 'Bluetooth-poc-ionic',
  webDir: 'www',
  bundledWebRuntime: false
};

export default config;
